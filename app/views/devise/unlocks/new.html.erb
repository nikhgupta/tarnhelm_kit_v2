<h1 class="heading">Resent unlock instructions</h1>
<%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "mt-6", data: { turbo: false } }) do |f| %>

    <div class="mt-10">
        <%= f.label :email, class: "form-control label-text" %>
        <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Email", class: "form-control form-field block w-full" %>
    </div>

    <%= f.submit "Resend unlock unstructions", class: "btn btn-primary block w-full mt-5" %>
<% end %>

#!/usr/bin/env sh

DISABLE_SPRING=1 RAILS_ENV=test bundle exec rails db:reset 
if [ $# -eq 0 ]; then
  DISABLE_SPRING=1 RAILS_ENV=test bundle exec rails test:all
else
  DISABLE_SPRING=1 RAILS_ENV=test bundle exec rails test $@
fi